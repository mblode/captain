---
"cmux-captain": minor
---

Add `captain status --all-projects` and `captain gain --all-projects`: one board across every project, with each row tagged by `project` and its `next` command naming the project, plus per-project and total gain numbers.
