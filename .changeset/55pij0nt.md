---
"cmux-captain": patch
---

Collapse setup to one command: `captain install` installs the skills the fleet needs (`mblode/captain` + the pipeline skills) and then checks the environment. Replaces `captain doctor` (the old check-only command, now removed).
