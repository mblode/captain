---
"cmux-captain": patch
---

`captain gain` now reports latency to detection: `start` ledgers each launch in `log.jsonl` (fail-soft, never `--print`) and gain joins launch→decision and launch→verdict by the qualified worktree name for median/max stats. The fleet-memory brief additionally requires distilling the root cause of any verifier failure once the run eventually passes.
