---
"cmux-captain": minor
---

Pin Codex workers to GPT-6 Sol (`gpt-6-sol`) by default, run reviews at high effort, and launch the Cursor CLI as `agent` (its current binary name), with `.harness.<name>.bin` in config for older installs.
