---
"cmux-captain": patch
---

Point the default pipeline at the current agent-skills names: `/tidy` replaces the retired `/pr-reviewer`, and `/ui-verification` replaces `/visual-qa`. `captain install` now fetches `tidy` instead of `pr-reviewer`.
