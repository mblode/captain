---
"cmux-captain": patch
---

Refuse planting issue worktrees in a `linear-god` checkout unless `--repo-path` is set, and make the driver loop work from outside cmux: honest ping errors (including cmux-only sockets), plan gates that join macOS `/private` cwds, and `/captain` skill/approve-with-note as the plan path.
